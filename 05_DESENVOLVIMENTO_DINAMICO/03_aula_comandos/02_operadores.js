// Cria variável numérica
let n = 3;

// Operadores aritméticos
n = n + 10; // Adiciona 10
n = n - 15; // Subtrai 15
n += 10; // Adiciona 10

// Unário de incremento e decremento
let x = 10;
x++; // Incrementa 1
x--; // Decrementa 1

// Operadores de comparação
console.log(n < x) // Compara os dois valores

// Operadores lógicos
let a = 10, b = 20
let c = 0, d = 0

// Faz duas comparações diferentes
if( a<b && c === d){// Operador AND (e)
    console.log("Verdadeiro")
}else{
    console.log("Falso")
}